#include "holberton.h"
/**
 *_abs - computes the absolute value of an interger
 *@n: The interger to check
 *Return: the absolute value of the interger
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
